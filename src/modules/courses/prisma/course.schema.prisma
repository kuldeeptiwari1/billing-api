model Course {
    code              String   @unique
    name              String
    departmentIds     String[] @default([]) // Corrected spelling
    branchIds         String[] @default([]) // Corrected spelling
    durationInMonths  Int
    courseFees        Float
    courseDescription String

    @@map("courses")
}
