model branch {
    name       String
    slug       String @unique
    code       String @unique
    address    String
    email      String
    contact    String
    closingDay Float  @default(5)

    @@map("branches")
}
